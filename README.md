# AOS

Landing page for AOS, an AI operating system for macOS and Windows.

One self-contained `index.html`: no build step, no dependencies, no external
requests. The crystal wordmark on the closing screen is drawn in WebGL from the
letterforms at load time, lit by the pointer.

Live: https://ryrxrrr.github.io/AOS/
